{
  "name": "glados-ai-animation",
  "version": "1.0.0",
  "description": "GLaDOS AI Animation Card for Home Assistant — modular source, bundled to a single HACS-distributable file",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node build.mjs && node --check glados-card.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "license": "MIT"
}